| Typ        | Wann verwenden?                                                     |
| ---------- | ------------------------------------------------------------------- |
| `feat`     | Neue Features, Funktionen oder Erweiterungen                        |
| `fix`      | Fehlerbehebungen (Bugfixes)                                         |
| `docs`     | Nur Dokumentation (README, Kommentare, Wiki…)                       |
| `style`    | Formatierungen (z. B. Leerzeichen, Einrückung, KEINE Code-Änderung) |
| `refactor` | Code umgebaut oder vereinfacht, ohne Verhalten zu ändern            |
| `test`     | Neue oder geänderte Tests (Unit, Integration…)                      |
| `chore`    | Routineaufgaben (z. B. Linter, Build-Tools, CI, Dependency-Updates) |
| `perf`     | Performance-Verbesserungen                                          |
| `build`    | Build-System oder Abhängigkeiten geändert (z. B. Webpack, npm)      |
| `ci`       | CI-Konfigurationen geändert (GitHub Actions, Travis, etc.)          |


function getComputerChoice:
    returns "Rock", "Paper" or "Scissors" randomly

function getHumanChoice:
    get human choice (toLower) and continue to prompt until valid

function playRound:
    